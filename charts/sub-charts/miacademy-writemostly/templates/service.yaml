apiVersion: v1
kind: Service
metadata:
  name: {{ include "miacademy-writemostly.fullname" . }}
  labels:
    {{- include "miacademy-writemostly.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "miacademy-writemostly.selectorLabels" . | nindent 4 }}
