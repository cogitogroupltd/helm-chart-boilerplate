apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "miacademy-main.fullname" . }}-test-connection"
  labels:
    {{- include "miacademy-main.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "miacademy-main.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
