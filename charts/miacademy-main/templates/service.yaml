apiVersion: v1
kind: Service
metadata:
  name: {{ include "miacademy-main.fullname" . }}
  labels:
    {{- include "miacademy-main.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "miacademy-main.selectorLabels" . | nindent 4 }}
